import { ActivityIndicator } from "react-native"
import React, {useState, useEffect} from 'react';
const LoadingComponent = (props)=>{
 return(
     <ActivityIndicator size = "large" color = "#FC0254"/>
 )
}

export default LoadingComponent;